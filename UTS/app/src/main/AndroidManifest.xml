<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mega.listalatmusiktradisional">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bc1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HalamanUtama" />
        <activity android:name=".ListAlatMusik" />
        <activity android:name=".ListYoutube" />
        <activity android:name=".TambahAlatMusik" />
        <activity android:name=".DetailAlatMusik" />
        <activity android:name=".UpdateAlatMusik"></activity>
    </application>

</manifest>